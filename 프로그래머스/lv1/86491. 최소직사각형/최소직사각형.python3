def solution(sizes):
    answer = 0
    for i in sizes :
        i.sort()
    max_w = 0
    max_h = 0
    for w, h in sizes :
        if(w > max_w) :
            max_w = w
        if(h > max_h) :
            max_h = h
    return max_w * max_h