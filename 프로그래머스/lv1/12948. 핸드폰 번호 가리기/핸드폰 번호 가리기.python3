def solution(phone_number):
    answer = ''
    l = len(phone_number);
    for i in range(l) :
        if l-4 > i :
            answer = answer + '*'
        else : 
            answer += phone_number[i]
    return answer